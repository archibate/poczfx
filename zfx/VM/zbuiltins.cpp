//
// Created by admin on 2022/7/7.
//
#include <cmath>
#include <time.h>

#define zfx_numAdd(a, b) ((a) + (b))
#define zfx_numSub(a, b) ((a) - (b))
#define zfx_numMul(a, b) ((a) * (b))
#define zfx_numDiv(a, b) ((a) / (b))

//判断指针是否相等
inline bool zfx_veceq() {

}
static int math_abs() {

}

static int math_sin() {

}